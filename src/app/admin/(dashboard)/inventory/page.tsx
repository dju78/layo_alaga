import InventoryClient from './InventoryClient';

export const metadata = {
  title: 'Inventory & Rentals | Admin – Alaga Alayo',
};

export default function InventoryPage() {
  return <InventoryClient />;
}
