import NewQuotationClient from './NewQuotationClient';

export const metadata = {
  title: 'Create Quotation | Admin – Alaga Alayo',
};

export default function NewQuotationPage() {
  return <NewQuotationClient />;
}
